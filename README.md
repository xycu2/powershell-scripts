# PowerShell Scripts

A collection of PowerShell scripts for daily workflow automation and task management.

## 📜 Available Scripts

- **`auto-push.ps1`** — Automates Git workflow (`git add`, `git commit`, `git push`).